# autk
Auditing Toolkit for auditors who are working hard but suffering a lot near the end of the year.
## GL Processing

## File Handling Assistant

### Find file script
Find file on your local disk with `Regular Expression`.
### Automatic Print
When you need to print a large sum of `Portable Document Format(PDF)` Files with `Adobe Acrobat DC`, this will help.
## Ipython Start-up Config
`ipython` is a marvellous shell. By configuring python scripts at `~/.ipython/profile_default/startup` you can initialize some `General Ledger Class` to speed up your work with `Microsoft Office Excel` files.
